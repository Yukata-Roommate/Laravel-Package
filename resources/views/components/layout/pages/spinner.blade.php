<div id="spinner" class="spinner-container">
    <div class="spinner-inner">
        <svg width="120" height="120" viewBox="-60 -60 120 120">
            <circle class="spinner" r="50" />
        </svg>

        <p class="spinner-text spinner-text-waiting">{{ __('message.spinner.waiting') }}</p>
        <p class="spinner-text spinner-text-loading">{{ __('message.spinner.loading') }}</p>
        <p class="spinner-text spinner-text-processing">{{ __('message.spinner.processing') }}</p>
        <p class="spinner-text spinner-text-saving">{{ __('message.spinner.saving') }}</p>
        <p class="spinner-text spinner-text-deleting">{{ __('message.spinner.deleting') }}</p>
        <p class="spinner-text spinner-text-fetching">{{ __('message.spinner.fetching') }}</p>
        <p class="spinner-text spinner-text-searching">{{ __('message.spinner.searching') }}</p>
        <p class="spinner-text spinner-text-submitting">{{ __('message.spinner.submitting') }}</p>
    </div>
</div>
